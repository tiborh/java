import java.util.ArrayList;

/**
 * Write a description of class ConsCached here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class ConsCached<E> extends Cons
{
    // instance variables - replace the example below with your own
    private int size;
    private ArrayList<E> the_list;

    /**
     * Constructor for objects of class ConsCached
     */
    public ConsCached(E e, ImList<E> rest)
    {
        super(e,rest);
        size = 0;
        the_list = new ArrayList<E>();
    }


}
